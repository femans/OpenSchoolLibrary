# OpenSchoolLibrary - Environment Variables

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================

# Supabase project URL (safe for client-side)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (safe for client-side)
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ OPTIONAL: Supabase service role key (LEGACY - being phased out by Supabase)
# Modern approach: Use RLS policies with authenticated users instead
# If you don't have this key (it's under "Legacy keys"), you can leave it blank
# The app will work with proper RLS policies configured
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# SINGLE-ORG MODE (for private Docker deployments)
# ============================================================================

# Enable single-org mode (set to 'true' for private installations)
SINGLE_ORG_MODE=true

# Single organization ID (use the UUID from your seeded organization)
# Default from migrations/seed-single-org.sql:
SINGLE_ORG_ID=00000000-0000-0000-0000-000000000000

# ============================================================================
# MULTI-TENANT MODE (for SaaS Vercel deployments)
# ============================================================================

# For multi-tenant SaaS deployments:
# 1. Set SINGLE_ORG_MODE=false (or remove the variable)
# 2. Remove or comment out SINGLE_ORG_ID
# 3. Implement org_id resolution from user session/JWT claims
# 4. Enable and configure RLS policies in Supabase

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# SvelteKit public base URL (for QR codes, email links, etc.)
# PUBLIC_BASE_URL=https://your-domain.com

# Node environment
# NODE_ENV=development
